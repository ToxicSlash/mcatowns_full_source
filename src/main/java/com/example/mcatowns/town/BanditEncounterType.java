package com.example.mcatowns.town;

/** Lightweight saved encounter kinds. Physical mobs/structures materialise only when players approach. */
public enum BanditEncounterType {
    WILD,
    TOWN,
    CAMP
}
